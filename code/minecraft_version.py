from hash_tables import ProtocolVersion

import logging


def check():
    # Todo: check is hash_tables. ProtocolVersion OK
    logging.warning(f"{__name__}.{check.__name__}() is not done yet!")
    return True


def minecraft_version_of(version_code: int = None,
                         version_enum: ProtocolVersion = None):
    pass


def protocol_version_of(version_enum: ProtocolVersion = None,
                        minecraft_version: str = None):
    pass


def protocol_enum_of(version_code: int = None,
                     minecraft_version: str = None):
    pass

